'use client';

import React from 'react';
import AdminDashboardPage from '../page'; // Reuse the existing overview which has the delegate list

export default function AdminDelegatesPage() {
    return <AdminDashboardPage />;
}
